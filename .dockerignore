# .dockerignore
#
# Prevents files that must never enter the Docker build context from being
# sent to the daemon. This keeps images small and secrets out of layers.
#
# Build is invoked from repo root:
#   docker build -f docker/Dockerfile -t artisthub-backend .

# ------------------------------------------------------------------ #
# Secrets — must NEVER be baked into the image                        #
# ------------------------------------------------------------------ #
.env

# ------------------------------------------------------------------ #
# Python artefacts — rebuilt inside the container                     #
# ------------------------------------------------------------------ #
**/__pycache__/
**/*.py[cod]
**/*.pyo
**/*.pyd
**/*.egg-info/
dist/
build/
.eggs/

# ------------------------------------------------------------------ #
# Virtual environment — the image installs its own deps via pip       #
# ------------------------------------------------------------------ #
backend/venv/
venv/
.venv/

# ------------------------------------------------------------------ #
# Database files — runtime data, not source code                      #
# ------------------------------------------------------------------ #
backend/instance/
*.db

# ------------------------------------------------------------------ #
# Test / CI artefacts — not needed in the production image            #
# ------------------------------------------------------------------ #
backend/tests/
backend/.coverage
backend/.pytest_cache/
coverage.xml
htmlcov/
.github/

# ------------------------------------------------------------------ #
# Editor and OS noise                                                  #
# ------------------------------------------------------------------ #
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ------------------------------------------------------------------ #
# Directories not needed in the backend image build context           #
# ------------------------------------------------------------------ #
frontend/
docker/
# Exclude kafka/ tooling but keep kafka/schemas/ — the Avro .avsc files
# are copied into the image at /app/kafka/schemas/ so the relay and
# consumers can load schemas without a Schema Registry round-trip on boot.
kafka/
!kafka/schemas/
scripts/
.bob/
.git/

# ------------------------------------------------------------------ #
# Documentation (not needed at runtime)                               #
# ------------------------------------------------------------------ #
README.md
AGENTS.md
artisthub-plan.md
kafka-architecture-plan.md
docker-containerize-plan.md
COMMIT_MSG.txt
